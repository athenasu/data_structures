"""
    Bubble sort function
"""


def bubble_sort(unsorted_list):
    """
        Sort list by swapping numbers that are bigger to the end of the list
        Steps:
            1. As bigger numbers are placed at the end, we will not need to sort the end of the list,
               so we stop counting from the back of the list
            2. Compare two numbers, if the one in front is bigger, then swap
            3. Return list
    """
    pass
            
 
my_list = [4, 6, 2, 5, 1, 3]

print(bubble_sort(my_list))