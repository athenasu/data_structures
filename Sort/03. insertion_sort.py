"""
    insertion sort function
"""

def insertion_sort(unsorted_list):
    """
        Find the smallest number and place at the beginning of the list
        Steps:
            1. Check two values, one at the current position and another before it
            2. If the one before it is smaller, then swap, 
               and continuing swapping with the one before that if it is still smaller
            3. Return list
    """
    pass

my_list = [4, 6, 2, 5, 1, 3]

print(insertion_sort(my_list))