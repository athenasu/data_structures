"""
    selection sort function
"""

def selection_sort(my_list):
    """
        Swap smallest number with first item in unsorted list
        Steps:
            1. find index with smallest number
            2. check if that index is equal to the current index
            3. if not, swap values
            4. return list
    """
    pass
 

my_list = [4, 6, 2, 5, 1, 3]

print(selection_sort(my_list))